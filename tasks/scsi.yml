---

- name: Create lookup table for disks
  ansible.builtin.set_fact:
    scsi_mapping: "{{ lookup('template', 'scsi-mapping.yml.j2') | from_yaml }}"

- name: Show lookup table
  ansible.builtin.debug:
    var: scsi_mapping
    verbosity: 1
