---

- hosts: localhost
  tasks:

    - name: xxx
      set_fact:
        docker_daemon: "{{ lookup('file', '/etc/docker/daemon.json') | from_json }}"

    - name: Show all facts
      debug:
        var: vars

    - name: xxx
      debug:
        var: docker_daemon
 
